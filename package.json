{
  "name": "timber-run",
  "version": "0.0.1",
  "scripts": {
    "start": "wp-env start && wp-env run cli \"wp theme activate theme\"",
    "update": "wp-env start --update"
  },
  "devDependencies": {
    "@wordpress/env": "^5.13.0"
  }
}
